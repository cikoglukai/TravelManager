apiVersion: v2
name: identity-tenant
description: Tenant + plan + white-label + SSO config service
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: travelmanager-service
    version: 0.1.0
    repository: file://../_template
